server:
  port: 8081

spring:
  application:
    name: user-service

  web:
    cors:
      mappings:
        "/**":
          allowed-origins: "*"
          allowed-methods: "*"
          allowed-headers: "*"

  datasource:
    url: jdbc:mysql://globalskill-globalskill.e.aivencloud.com:23246/user-service?ssl-mode=REQUIRED
    username: avnadmin
    password: ${AIVEN_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  main:
    allow-circular-references: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: quanonepunch@gmail.com
    password: ${SPRING_MAIL_PASSWORD}
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          timeout: 10000
          connectiontimeout: 10000

postmark:
  api:
    token: ${TOKEN_POSTMARK}
  sender:
    email: quandmse183805@fpt.edu.vn


  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML