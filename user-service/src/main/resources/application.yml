server:
  port: 8081

spring:
  application:
   name : user-service

  web:
    cors:
      mappings:
        "/**":
          allowed-origins: "*"
          allowed-methods: "*"
          allowed-headers: "*"

  datasource:
    url: jdbc:mysql://globalskill-globalskill.e.aivencloud.com:23246/user-service?ssl-mode=REQUIRED
    username: avnadmin
    password: ${AIVEN_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  main:
    allow-circular-references: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: quandmse183805@fpt.edu.vn
    password: rovi zpqp qlhv ynva
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    default-encoding: UTF-8

    thymeleaf:
      prefix: classpath:/templates/
      suffix: .html
      mode: HTML